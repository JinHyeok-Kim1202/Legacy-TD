# Schema Notes

- `unit.schema.json` defines shared unit content contracts.
- `enemy.schema.json` defines standard enemy records.
- `recipe.schema.json` defines merge recipes.
- `wave.schema.json` defines round wave payloads.
- `difficulty.schema.json` defines difficulty tuning and leak limits.
- `board.schema.json` defines static board and path metadata.
